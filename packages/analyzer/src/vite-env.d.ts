/// <reference types="vite/client" />
//
// Standard Vite ambient types. Provides `import.meta.glob` and the module
// declarations for asset query suffixes (`*.svg?raw` resolves to `string`),
// both used by the /architecture route and its coverage test.
