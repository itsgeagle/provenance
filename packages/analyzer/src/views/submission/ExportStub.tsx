/**
 * Export tab — placeholder.
 *
 * Phase 24 adds the export panel (POST /export + poll + download).
 */

export function ExportStub() {
  return (
    <div className="container mx-auto py-12 text-center text-gray-400" data-testid="export-stub">
      <p className="text-lg font-medium">Export — Coming in next sprint</p>
      <p className="text-sm mt-2">Export panel (PDF, Markdown, bundle download) is Phase 24.</p>
    </div>
  );
}
