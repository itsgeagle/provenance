/**
 * Timeline placeholder — Phase 7 will replace this with the real view.
 */

export function TimelinePlaceholder() {
  return (
    <div
      data-testid="timeline-placeholder"
      className="flex min-h-[calc(100vh-3.5rem)] items-center justify-center text-muted-foreground"
    >
      <p className="text-lg">Timeline — coming next phase</p>
    </div>
  );
}
