/**
 * Overview placeholder — Phase 6 will replace this with the real view.
 */

export function OverviewPlaceholder() {
  return (
    <div
      data-testid="overview-placeholder"
      className="flex min-h-[calc(100vh-3.5rem)] items-center justify-center text-muted-foreground"
    >
      <p className="text-lg">Overview — coming next phase</p>
    </div>
  );
}
